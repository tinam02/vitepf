import { ThemeOptions } from "@mui/material/styles";

const baseTheme: ThemeOptions = {
  typography: {
    fontFamily: ["Tan", "Roboto", "Times New Roman"].join(","),
  },
};

export default baseTheme;
